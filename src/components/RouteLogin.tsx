import React from 'react';
import { RouteComponentProps } from '@reach/router';

const RouteLogin = (_: RouteComponentProps) => (
  <>
    <h1>Log In or Sign Up</h1>
    <button>Log In</button>
  </>
);

export default RouteLogin;
